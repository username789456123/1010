#include <stdio.h>
int main()
{
	int a,n;
	int flag=0;
	char ch;
	while(1){
		scanf("%d %d",&a,&n);
		if(n!=0){
			if(flag==1) 
			printf("");
			printf("%d %d",a*n,n-1);
			flag=1;
		}
		ch=getchar();
		if(ch=='\n')
		break;
	}
	if(flag==0)
	printf("0 0");
}
